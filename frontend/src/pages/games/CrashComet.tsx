import CrashGameBase from '../../components/games/CrashGameBase';

const CrashComet = () => {
  return (
    <CrashGameBase
      gameTitle="Crash Comet"
      gameIcon="☄️"
      backgroundColor="#0a0a2e"
      animationType="comet"
      gameType="crashcomet"
    />
  );
};

export default CrashComet;
